<!-- dashboardHeader.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        /* COLOR SCHEME UPDATE */
        :root {
            --primary: #0f766e; /* Teal */
            --secondary: #facc15; /* Yellow */
            --card-bg: #ffffff;
            --page-bg: #f8fafc;
            --text-dark: #111827;
            --text-light: #ffffff;
        }

        body {
            background: var(--page-bg);
            font-family: 'Inter', sans-serif;
            color: var(--text-dark);
        }

        /* COMPACT NAVBAR */
        .admin-navbar {
            background: linear-gradient(90deg, #134e4a, #0f766e);
            color: var(--text-light);
            box-shadow: 0 4px 12px rgba(16, 24, 40, 0.08);
            padding: 0.4rem 1rem;
        }

        .admin-navbar .nav-link {
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.9rem;
            padding: 0.4rem 0.7rem;
            transition: 0.2s;
        }

        .admin-navbar .nav-link.active,
        .admin-navbar .nav-link:hover {
            font-weight: 600;
            color: var(--secondary);
        }

        .admin-container {
            max-width: 1200px;
            margin: 20px auto;
        }

        /* CARDS */
        .stat-card,
        .table-card,
        .form-card {
            border-radius: 12px;
            background: var(--card-bg);
            box-shadow: 0 6px 22px rgba(0, 0, 0, 0.05);
            transition: 0.3s ease;
        }

        .stat-card:hover,
        .table-card:hover,
        .form-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
        }

        /* BUTTONS */
        .btn-brand {
            background: var(--primary);
            color: #fff;
            border: none;
            transition: 0.2s;
        }

        .btn-brand:hover {
            background: #065f55;
        }

        /* FLASH MESSAGES */
        .alert {
            border-radius: 10px;
            padding: 10px 18px;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.9rem;
        }

        .alert-success {
            background-color: #d1fae5;
            color: #065f46;
        }

        .alert-danger {
            background-color: #fee2e2;
            color: #b91c1c;
        }

        .alert button.btn-close {
            margin-left: auto;
        }

        /* TABLE STYLING */
        table.table th {
            background-color: #ccfbf1;
            color: var(--text-dark);
        }

        table.table td,
        table.table th {
            vertical-align: middle;
        }

        table.table tr:hover {
            background-color: #f1f5f9;
        }
    </style>
</head>

<body>
    <nav class="navbar admin-navbar navbar-expand-lg">
        <div class="container admin-container">
            <a class="navbar-brand" href="/admin/dashboard">Admin Panel</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNav">
                <span class="navbar-toggler-icon"><i class="fa fa-bars" style="color:#fff"></i></span>
            </button>
            <div class="collapse navbar-collapse" id="adminNav">
                <ul class="navbar-nav ms-auto align-items-lg-center">
                    <li class="nav-item"><a class="nav-link" href="/admin/dashboard">Dashboard</a></li>
                    <li class="nav-item"><a class="nav-link" href="/admin/restaurants">Restaurants</a></li>
                    <li class="nav-item"><a class="nav-link" href="/admin/menus">Menus</a></li>
                    <li class="nav-item"><a class="nav-link" href="/admin/reservations">Reservations</a></li>
                    <li class="nav-item ms-3"><span class="nav-link text-warning">Welcome, <%= user ? user.fullName || user.username : 'Admin' %></span></li>
                    <li class="nav-item ms-2"><a class="btn btn-sm btn-danger" href="/logout">Logout</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="admin-container mt-3">
        <% if (locals.success_msg && locals.success_msg.length > 0) { %>
            <div class="alert alert-success alert-dismissible fade show">
                <i class="fa fa-check-circle"></i> <%= success_msg %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <% if (locals.error_msg && locals.error_msg.length > 0) { %>
            <div class="alert alert-danger alert-dismissible fade show">
                <i class="fa fa-exclamation-triangle"></i> <%= error_msg %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>
